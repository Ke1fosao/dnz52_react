# ============================================================================
# Залежності ЗДО №52 — версії перевірені та сумісні з Python 3.11+
# ============================================================================

# Основний фреймворк
Django==5.2.15

# Безпечне завантаження змінних з .env
python-dotenv==1.2.2

# Картинки (потрібно для ImageField)
pillow==12.2.0

# Markdown-редактор для адмінки (замінив django-ckeditor — легший, менше статики)
# (django-markdownx нижче у секції адмінки)

# Гарні форми
django-crispy-forms==2.3
crispy-bootstrap5==2024.10

# Витяг тексту з PDF (для seed-скриптів)
pypdf==6.11.0

# Допоміжні бібліотеки Django
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# ============================================================================
# Продакшен-залежності (для деплою на хостинг)
# ============================================================================

# WSGI-сервер для продакшену (Render, Railway, Fly.io тощо)
# На PythonAnywhere не потрібен, але хай буде — не заважає
gunicorn==23.0.0

# Роздача статики (CSS/JS/SVG) безпосередньо Django.
# При DEBUG=False сервер не віддає статику — це робить whitenoise
whitenoise[brotli]==6.8.2

# Парсинг DATABASE_URL — потрібно для Render/Railway/Heroku-подібних
dj-database-url==2.3.0

# PostgreSQL-драйвер (Supabase). psycopg 3 збирається на Python 3.11–3.14
# (psycopg2-binary НЕ збирається на 3.14). Django 5 нативно підтримує psycopg 3.
psycopg[binary]==3.2.13

# ============================================================================
# Supabase: віддалена БД (PostgreSQL через пулер) + сховище медіа (S3 Storage)
# ============================================================================
# Медіа зберігаються у Supabase Storage через django-storages (S3 backend).
# Вмикається лише якщо у .env задано AWS_STORAGE_BUCKET_NAME (інакше — локальні файли).
django-storages==1.14.4
boto3==1.35.99

# ============================================================================
# REST API (для React фронтенду dnz52-react)
# ============================================================================
djangorestframework==3.15.2
django-cors-headers==4.6.0
django-filter==24.3

# Web-push сповіщення (VAPID)
pywebpush==2.0.0

# ============================================================================
# Адмінка: версійність, drag-drop сортування, markdown-редактор
# ============================================================================
django-simple-history==3.11.0
django-admin-sortable2==2.3.1
django-markdownx==4.0.11

# ============================================================================
# Безпека: брутфорс-захист, CSP, 2FA для адмінки
# ============================================================================
django-axes==8.3.1          # ліміт невдалих спроб входу в адмінку
django-csp==4.0             # заголовки Content-Security-Policy
django-otp==1.7.0           # 2FA (TOTP) для адмінки (env-gated: ENFORCE_ADMIN_2FA)
qrcode==8.2                 # QR-код для підключення TOTP-пристрою
bleach==6.2.0               # санітизація HTML (ШІ-генерація, поля контенту)

# ============================================================================
# Тестування та якість коду
# ============================================================================
pytest>=8.0
pytest-django>=4.10
pytest-cov>=6.0
