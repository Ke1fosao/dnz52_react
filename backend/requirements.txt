# ============================================================================
# Залежності ЗДО №52 — версії перевірені та сумісні з Python 3.11+
# ============================================================================

# Основний фреймворк
Django==5.2.14

# Безпечне завантаження змінних з .env
python-dotenv==1.2.2

# Картинки (потрібно для ImageField)
pillow==12.2.0

# Markdown-редактор для адмінки (замінив django-ckeditor — легший, менше статики)
# (django-markdownx нижче у секції адмінки)

# Гарні форми
django-crispy-forms==2.3
crispy-bootstrap5==2024.10

# Витяг тексту з PDF (для seed-скриптів)
pypdf==6.11.0

# Допоміжні бібліотеки Django
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# ============================================================================
# Продакшен-залежності (для деплою на хостинг)
# ============================================================================

# WSGI-сервер для продакшену (Render, Railway, Fly.io тощо)
# На PythonAnywhere не потрібен, але хай буде — не заважає
gunicorn==23.0.0

# Роздача статики (CSS/JS/SVG) безпосередньо Django.
# При DEBUG=False сервер не віддає статику — це робить whitenoise
whitenoise==6.8.2

# Парсинг DATABASE_URL — потрібно для Render/Railway/Heroku-подібних
dj-database-url==2.3.0

# PostgreSQL-драйвер (для Render/Railway, які пропонують postgres безкоштовно)
# На PythonAnywhere не обовʼязково — там SQLite або MySQL
# УВАГА: psycopg2-binary не зібрається на Python 3.14, але для локального запуску
# на SQLite він не потрібен. Розкоментуйте лише якщо деплоїте з PostgreSQL.
# psycopg2-binary==2.9.10

# ============================================================================
# REST API (для React фронтенду dnz52-react)
# ============================================================================
djangorestframework==3.15.2
django-cors-headers==4.6.0
django-filter==24.3

# Web-push сповіщення (VAPID)
pywebpush==2.0.0

# ============================================================================
# Адмінка: версійність, drag-drop сортування, markdown-редактор
# ============================================================================
django-simple-history==3.11.0
django-admin-sortable2==2.3.1
django-markdownx==4.0.11
